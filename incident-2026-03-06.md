# 故障处理记录

## 时间
2024-07-04

## 现象
Redis容器异常退出，导致玩家登录验证失败。

## 排查过程
1. `docker ps` 发现redis-test容器状态为Exited
2. `docker logs redis-test` 查看日志，发现内存不足导致OOM
3. 重启容器恢复正常

## 解决方案
- 短期：重启服务 `docker start redis-test`
- 长期：优化Redis内存配置，增加ECS内存或限制Redis内存使用

## 用到命令
docker ps, docker logs, docker start
